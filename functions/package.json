{
  "name": "pfa-seat-booking-functions",
  "description": "Seat booking backend for People for Animals fundraisers",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,auth",
    "logs": "firebase functions:log",
    "deploy": "firebase deploy --only functions",
    "test": "node ../tests/integration.test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "nodemailer": "^6.9.15",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.3.0"
  }
}